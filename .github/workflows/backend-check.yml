name: DiroX backend check
on:
  workflow_dispatch:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node --check api/server.js
      - run: node --check app.js
      - run: npm start & pid=$!; sleep 2; curl --fail http://localhost:3000/health; kill $pid
